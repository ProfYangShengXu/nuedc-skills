# 全国大学生电子设计竞赛 (TI杯) 完整工作流

> **从拿到赛题到交作品 — agent 全程辅助，你只负责动手**

---

## 工作流总览

```
  ┌─────────────────────────────────────────────────────────┐
  │  Phase 0: 拿到赛题          耗时: 30分钟                │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │  解析题目 → 提取指标 → 确定题型 → 识别TI芯片   │   │
  │  └─────────────────────────────────────────────────┘   │
  │                          ↓                             │
  │  Phase 1: 方案设计          耗时: 2小时                │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │  系统框图 → 模块划分 → 芯片选型 → 引脚分配     │   │
  │  └─────────────────────────────────────────────────┘   │
  │                          ↓                             │
  │  Phase 2: 硬件设计          耗时: 1天                 │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │  SVG原理图 → PCB布局 → KiCad自动摆放 → 布线    │   │
  │  │  → Gerber导出 → 下单打样                       │   │
  │  └─────────────────────────────────────────────────┘   │
  │                          ↓                             │
  │  Phase 3: 焊接装配          耗时: 1天                 │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │  按照接线图焊接 → 万用表测短路 → 上电测试      │   │
  │  └─────────────────────────────────────────────────┘   │
  │                          ↓                             │
  │  Phase 4: 软件开发          耗时: 2天                 │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │  Keil项目生成 → 外设驱动 → 核心算法 → 编译    │   │
  │  │  → 烧录 → 串口调试                            │   │
  │  └─────────────────────────────────────────────────┘   │
  │                          ↓                             │
  │  Phase 5: 系统联调          耗时: 1天                 │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │  逐项测试指标 → 调参优化 → 压力测试 → 记录    │   │
  │  └─────────────────────────────────────────────────┘   │
  │                          ↓                             │
  │  Phase 6: 文档答辩          耗时: 0.5天               │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │  设计报告 → 测试数据 → 答辩PPT → 排练         │   │
  │  └─────────────────────────────────────────────────┘   │
  └─────────────────────────────────────────────────────────┘
```

---

## Phase 0: 赛题解析 (30分钟)

### 触发方式

```bash
# 在 Reasonix/Claude Code/Codex 中:
run_skill("nuedc", arguments="分析2025年A题 能量回馈的变流器负载试验装置")
```

### agent 自动输出

```
╔══════════════════════════════════════════════╗
║  2025年 A题：能量回馈的变流器负载试验装置   ║
║                                             ║
║  🎯 核心任务: Buck-Boost 背靠背能量回馈     ║
║                                             ║
║  📐 基本要求:                               ║
║    ① 恒流模式: 电流连续可调                ║
║    ② 恒压模式: 电压可调                    ║
║    ③ 效率 ≥ 85%                            ║
║                                             ║
║  ⭐ 发挥部分:                               ║
║    ① 动态响应 < 100μs                      ║
║    ② 可编程负载曲线                        ║
║    ③ 上位机显示                            ║
║                                             ║
║  🔌 指定芯片: TMS320F28027 (C2000)         ║
╚══════════════════════════════════════════════╝
```

### 题型判断树

```
电源类 ─→ 检查: 效率/纹波/动态响应/拓扑结构
控制类 ─→ 检查: 传感器精度/控制周期/执行器
测控类 ─→ 检查: ADC精度/采样率/信号调理
高频类 ─→ 检查: 中心频率/带宽/阻抗匹配
通信类 ─→ 检查: 调制方式/速率/误码率
无人机 ─→ 检查: 姿态传感器/PWM/光流
综测   ─→ 检查: 运放电路/焊接/波形
```

---

## Phase 1: 方案设计 (2小时)

### 1.1 系统框图生成

agent 根据赛题类型自动生成系统框图：

```bash
# 电源类: Buck-Boost 双向变换
┌─────────┐   ┌──────────┐   ┌──────────┐   ┌─────────┐
│ 前级电源 ├──→│ Buck-Boost├──→│ Buck-Boost├──→│ DC-AC   │
│ 12V DC   │   │ #1 (CC)   │   │ #2 (CV)   │   │ 逆变    │
└─────────┘   └────┬─────┘   └────┬─────┘   └─────────┘
                    │ PWM          │ PWM
                    ↓              ↓
               ┌──────────────────────┐
               │ MCU: TMS320F28027    │
               │ ePWM × 4 + ADC × 4  │
               └──────────┬───────────┘
                          │ UART
                     ┌────┴────┐
                     │ 上位机   │
                     └─────────┘

# 控制类: 智能小车
┌──────────┐   ┌──────────┐
│ 循迹模块  ├──→│          │   ┌──────────┐
│ TCRT5000  │   │          │   │ L298N    │
├──────────┤   │   MCU    ├──→│ 电机驱动  │
│ 超声波    ├──→│ STM32F1  │   └──────────┘
│ HC-SR04   │   │          │
├──────────┤   │          │   ┌──────────┐
│ 编码器    ├──→│          │   │ OLED     │
└──────────┘   └──────────┘   └──────────┘
```

### 1.2 芯片选型

| 类别 | 首选 TI 芯片 | 备选 | 原因 |
|------|-------------|------|------|
| 主控(电源) | TMS320F28027 | STM32F103 | ePWM高精度, 12位ADC |
| 主控(控制) | MSPM0G3507 | STM32F103C8 | 便宜, 资料多 |
| 主控(测控) | TMS320F28377 | STM32F407 | DSP性能, 浮点运算 |
| 驱动 | IR2104 | IR2110 | 半桥驱动, 自举 |
| MOS | IRF3205 | IRFZ44N | 55V/110A |
| 运放 | LMV358 | TLV2372 | 轨到轨, 3MHz |
| 比较器 | LM393 | TLV3201 | 高速比较 |
| LDO | AMS1117-3.3 | TPS7A47 | 3.3V/1A |
| 隔离 | ISO7240 | ADuM1201 | 数字隔离 |

### 1.3 引脚分配表 (以智能送药小车为例)

| 功能 | MCU引脚 | 对端器件 | 对端引脚 | 线色 |
|------|---------|---------|---------|------|
| 左电机PWM | PA0 | L298N ENA | Pin 1 | 黄 |
| 左电机IN1 | PA1 | L298N IN1 | Pin 2 | 绿 |
| 左电机IN2 | PA2 | L298N IN2 | Pin 3 | 蓝 |
| 右电机PWM | PA3 | L298N ENB | Pin 4 | 橙 |
| 循迹1 | PA6 | TCRT5000 D0 | OUT | 紫 |
| 循迹2 | PA7 | TCRT5000 D0 | OUT | 灰 |
| 超声波TRIG | PB10 | HC-SR04 TRIG | Trig | 白 |
| 超声波ECHO | PB11 | HC-SR04 ECHO | Echo | 棕 |
| OLED SCL | PB6 | OLED SCL | SCL | 紫 |
| OLED SDA | PB7 | OLED SDA | SDA | 灰 |
| ST-LINK | PA13 | ST-Link SWDIO | SWDIO | 黄 |
| ST-LINK | PA14 | ST-Link SWCLK | SWCLK | 绿 |
| VCC | 3.3V | ST-Link 3.3V | 3.3V | 红 |
| GND | GND | ST-Link GND | GND | 黑 |

---

## Phase 2: 硬件设计 (1天)

### 2.1 原理图生成

```bash
# agent 调用: 生成 Buck 电路 SVG 原理图
python nuedc-pcb-svg.py
# 输出 → wiring-diagrams/buck_12to3v3.svg
#        wiring-diagrams/boost_5to12.svg

# agent 调用: 计算电路参数
python nuedc-pcb-gen.py buck --vin 12 --vout 3.3 --iout 2 --fsw 500k
# 输出: D=0.275 L=9.6uH Cout=33.3uF
```

生成的 SVG 原理图包含：
- 完整元件符号（电阻/电容/电感/MOS管/二极管/IC）
- 连接导线
- 参数计算表（占空比/电感值/电容值/耐压）

### 2.2 PCB 自动布局

```bash
# agent 调用: 生成 KiCad 自动布局脚本
python nuedc-pcb-place.py buck --kicad-script

# 在 KiCad 中运行 (需要 KiCad 10.0+)
# "C:\Program Files\KiCad\10.0\bin\python.exe" wiring-diagrams/kicad_auto_buck.py
# → 自动生成 auto_placed.kicad_pcb (元件已摆好)
```

元件摆放规则：
```
功率回路: 输入电容 → MOS管 → 电感 → 二极管 → 输出电容
          (走线短粗, 20-40mil)
控制IC:  靠近MOS管驱动引脚, 远离电感磁场
反馈:    靠近IC反馈引脚, 走线远离功率回路
连接器:  板边缘, 方向统一对外
去耦电容: 每个IC引脚旁 ≤5mm 放一个 100nF
GND:     底层覆铜, 关键信号下方不要割裂
```

### 2.3 自动布线 → Gerber

```bash
# 在 KiCad PCB Editor 中:
# Route → Auto-route → Route All
# 或者 CLI (KiCad 10.0+):
kicad-cli pcb autoroute auto_placed.kicad_pcb -o routed.kicad_pcb

# 导出 Gerber (JLCPCB 打样)
kicad-cli pcb export gerber routed.kicad_pcb --output gerber/
kicad-cli pcb export drill routed.kicad_pcb --output gerber/

# DRC 检查
kicad-cli pcb drc routed.kicad_pcb --output drc.rpt

# 打样: 将 gerber/ 文件夹打包 zip, 上传 JLCPCB
# 参数: 2层 / 1.6mm / 1oz铜 / 绿色 / 5片 / ¥20-50 / 3-4天
```

### 2.4 BOM 导出 + 采购

```bash
# agent 生成 BOM (含立创商城料号)
```

| 位号 | 型号 | 规格 | 数量 | 立创料号 | 参考价 |
|------|------|------|:---:|---------|:------:|
| U1 | TMS320F28027 | TQFP-48 | 1 | C181138 | ¥35 |
| Q1 | IRF3205 | TO-220 | 2 | C4294 | ¥3 |
| D1 | SS34 | SMA | 2 | C8673 | ¥0.5 |
| L1 | CD54 10uH | 功率电感 | 2 | C95901 | ¥1.5 |
| C1,C2 | 10uF/25V | 1206 MLCC | 4 | C15850 | ¥0.3 |
| R1,R2 | 10k 1% | 0805 | 4 | C25487 | ¥0.1 |

---

## Phase 3: 焊接装配 (1天)

### 3.1 按图接线

```bash
# agent 生成接线图 SVG
python keil-wiring-diagram.py --mcu STM32F103C8 --peripherals "LED:PB0,MOTOR:PA0-PA5,UART:PA9-PA10" --lang zh
# 输出 → wiring-diagrams/wiring_STM32F103C8.svg (浏览器打开)
```

### 3.2 焊接标准流程

```
烙铁: 350°C ± 10°C  焊锡: 63/37 含铅
直插: 加热焊盘2秒 → 送锡 → 浸润 → 撤锡 → 保持3秒凝固
SMD(拖焊): 上锡到一排引脚 → 拖锡 → 助焊剂清洗

检查:
  ✅ 焊点呈锥形, 光亮
  ❌ 球形=冷焊, 锡珠=短路, 没爬到焊盘=虚焊

上电前必做:
  □ 万用表蜂鸣档测 VCC↔GND → 不响 (响了=短路!)
  □ 目测焊点无桥接
  □ 电解电容方向正确
  □ IC方向正确 (缺口/白点对齐)
```

---

## Phase 4: 软件开发 (2天)

### 4.1 Keil 项目生成

```bash
# agent 生成工程模板
# 包含: main.c + main.h + Makefile + 各外设驱动

# 编译
UV4.exe -b project.uvprojx -j0

# 日志分析
python keil-log-analyzer.py build.log --summary
```

### 4.2 代码框架 (以智能送药小车为例)

```c
int main(void)
{
    HAL_Init();
    SystemClock_Config();       // 72MHz
    
    MX_GPIO_Init();             // 电机IO + 循迹IO
    MX_TIM2_Init();             // PWM 20kHz
    MX_TIM3_Init();             // 编码器 4倍频
    MX_USART1_UART_Init();      // 串口 115200
    MX_I2C1_Init();             // OLED I2C
    
    Sensor_Init();              // 超声波 + 循迹
    Motor_Init();               // 电机PID参数
    Display_Init();             // OLED欢迎界面
    
    // === 赛题核心算法 ===
    while (1)
    {
        // 1. 采集传感器
        line_sensor_read(track);        // 5路循迹值
        distance = ultrasonic_read();   // 障碍物距离
        encoder_read(&left_speed, &right_speed);
        
        // 2. PID 计算
        pid_error = calc_line_error(track);
        pid_output = pid_update(&pid_line, pid_error);
        
        // 3. 避障判断
        if (distance < 15) {            // 15cm 有障碍
            motor_stop();
            HAL_Delay(500);
            motor_avoid();              // 绕障
        } else {
            motor_set_speed(pid_output);
        }
        
        // 4. 病房号识别
        if (detect_room_number(&room)) {
            if (room == target_room) {
                motor_stop();
                display_show("送药完成!", room);
                UART_Send("ARRIVED\n");
                break;
            }
        }
        
        // 5. 显示
        display_update(speed, distance, room);
        HAL_Delay(10);                  // 100Hz 控制周期
    }
}
```

### 4.3 调试方法

```
串口助手 (115200 8N1):
  'w' → 前进   's' → 后退
  'a' → 左转   'd' → 右转
  ' ' → 急停
  自动上报: speed=123 distance=45 track=01101

示波器:
  CH1:  PWM 输出 (PA0) → 20kHz 方波
  CH2:  超声波回波 (PB11) → 脉宽=距离
  CH1-CH2: 编码器 AB 相 → 测速

GDB:
  break main.c:42
  print pid_error
  continue
```

---

## Phase 5: 系统联调 (1天)

### 5.1 逐项指标测试

```
┌──────────────────────────────────────────────────────────┐
│  测试记录表                                           │
│                                                        │
│  基本要求 ①: 沿黑线循迹行驶                           │
│  ☐ 直道循迹: ____/10次成功                           │
│  ☐ 弯道循迹: ____/10次成功                           │
│                                                        │
│  基本要求 ②: 识别病房数字 (1-8)                       │
│  ☐ 数字1: □  □  □  □  □ (5次识别)                   │
│  ☐ 数字2: □  □  □  □  □                               │
│                                                        │
│  基本要求 ③: 载重 200g                                 │
│  ☐ 空载循迹: ______ 秒                                 │
│  ☐ 载重循迹: ______ 秒 (≤空载×1.3)                   │
│                                                        │
│  发挥部分 ①: 避障                                     │
│  ☐ 障碍物 5cm: □  □  □  □  □                          │
│  ☐ 障碍物 15cm: □  □  □  □  □                         │
│                                                        │
│  发挥部分 ②: 多车协同                                 │
│  ☐ 单车完成: ______ 秒                                │
│  ☐ 双车完成: ______ 秒                                │
└──────────────────────────────────────────────────────────┘
```

### 5.2 常见问题排障

| 现象 | 可能性 | 检查步骤 |
|------|--------|----------|
| 电机不动 | 供电/使能/占空比 | 1. L298N 5V灯亮? 2. ENA/ENB接高? 3. PWM≥10%? |
| 循迹乱跑 | 传感器阈值/安装高度 | 1. 串口看数值 2. 调节电位器 3. 离地5-8mm |
| OLED不亮 | I2C地址/接线 | 1. 地址是0x3C还是0x3D? 2. SCL/SDA接对? |
| 超声波不准 | 供电/声波干扰 | 1. 5V供电 2. 手放10cm看读数 3. 避开电机PWM噪声 |
| 烧录失败 | 接线/驱动/芯片锁 | 1. ST-Link红灯? 2. SWDIO/SWCLK接对? 3. 按住复位烧录 |

### 5.3 参数整定 (PID)

```
1. 先调 Kp (比例):
   Kp=0.5 → 观察 → 如果震荡严重 → Kp减半
   Kp=0.5 → 观察 → 如果响应太慢 → Kp×1.5

2. 再调 Kd (微分):
   Kd=Kp×0.1 → 观察 → 震荡减小?
   过冲大 → 增加 Kd

3. 最后调 Ki (积分):
   Ki=Kp×0.01 → 消除静差
   如果积分饱和 → 限幅 ±100

口诀: 先P后D再I, 一点一点加, 边加边看波形
```

---

## Phase 6: 文档答辩 (0.5天)

### 6.1 设计报告框架

agent 自动生成设计报告框架：

```
1. 引言
   - 赛题要求
   - 系统方案选择
   
2. 方案设计
   - 系统框图
   - 模块选型理由
   - 参数计算过程
   
3. 硬件设计
   - 原理图
   - PCB 布局
   - 关键电路分析
   
4. 软件设计
   - 程序流程图
   - 核心算法 (PID/FFT等)
   - 模块化设计
   
5. 测试数据
   - 逐项指标测试记录
   - 波形截图
   - 效率/精度数据表
   
6. 总结
   - 完成情况
   - 改进方向
   - 心得体会
```

### 6.2 答辩 checklist

```
□ 作品能正常演示 (基本要求全部跑通)
□ 发挥部分至少完成 1-2 项
□ 设计报告打印 3 份
□ 波形/数据截图贴在报告里
□ 准备好 3 分钟讲解
□ 准备好 2 分钟问答 (方案选择/参数计算/改进方向)
□ 备用电池/芯片/下载器
□ 代码 U 盘备份 × 2
```

---

## 附录: 电赛 4 天倒计时

```
Day 1 (8:00 - 22:00):
  ────────
  8:00  拿到赛题, 团队讨论选哪题 (30分钟)
  8:30  agent分析赛题, 出方案 (30分钟)
  9:00  确定方案, 分配任务 (1小时)
  10:00 下单采购 (如果有现成的跳过)
  12:00 开始焊接/搭电路
  20:00 写完代码框架, 编译通过
  22:00 收工休息

Day 2 (8:00 - 23:00):
  ────────
  8:00  继续焊接/接线
  12:00 硬件基本完成, 开始软硬件联调
  14:00 调通基本要求第①项
  18:00 基本要求全部跑通
  23:00 收工休息

Day 3 (8:00 - 24:00):
  ────────
  8:00  优化基本要求指标
  12:00 开始做发挥部分
  18:00 发挥部分跑通
  22:00 反复测试, 记录数据
  24:00 收工休息

Day 4 (8:00 - 15:00):
  ────────
  8:00  最终测试, 所有指标记录
  10:00 写设计报告
  12:00 做答辩准备
  14:00 备份所有代码和数据
  15:00 封箱交作品!

⚠️ 千万别做的事:
  ❌ 第一天上午换方案 (选了就干到底)
  ❌ 最后一天改代码 (改出 bug 没时间修)
  ❌ 不备份 (ST-Link 坏了/电脑蓝屏 → 全完)
  ❌ 不准备备件 (MOS管烧了没得换)
  ❌ 上电前不测短路 (冒烟=0分)
```

---

> 📌 **工作流版本**: v2.0 | **更新**: 2025-08
> 🎯 覆盖: 6个Phase · 11个子模块 · 4天倒计时
> 🔗 关联技能: keil5 / nuedc / kicad
> 🔧 关联工具: keil-wiring-diagram / nuedc-pcb-gen / nuedc-pcb-place / keil-log-analyzer / nuedc-fetcher
